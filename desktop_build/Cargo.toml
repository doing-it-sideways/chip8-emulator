[package]
name = "desktop_build"
version = "0.1.0"
edition = "2024"

[dependencies]
chip8 = { path = "../" }
sdl3 = { version = "0.17.3", features = ["build-from-source", "raw-window-handle", "unsafe_textures"] }
clap = { version = "4.6.1", features = ["derive"] }