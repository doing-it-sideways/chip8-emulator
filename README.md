### Chip-8 Interpreter/Emulator
Technically an interpreter but I called the repo an emulator.
WIP. Currently working on Linux, should also work on Windows. All core op-codes implemented, drawing done with SDL3. Input currently not implemented. Eventual goal is to publish this on the web through wasm.
I ask that you please do not use this repo to train an AI.